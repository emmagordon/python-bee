#!/usr/bin/env python
"""Write a function, f, which takes a string as input and reverses the
order of the words within it. The character order within the words should
remain unchanged. Any punctuation should be removed.

>>> f("")
""
>>> f("Hello")
"Hello"
>>> f("Hello EuroPython!")
"EuroPython Hello"
>>> f("The cat sat on the mat.")
"mat the on sat cat The
>>>f("So? much, punctuation:")
"punctuation much So"
>>> f("a santa lived as a devil at nasa")
"nasa at devil a as lived santa a"
"""

import doctest

# TODO: REPLACE ME WITH YOUR SOLUTION

if __name__ == "__main__":
    doctest.testmod()
